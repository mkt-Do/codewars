# Codewars by Ruby (For @mkt-Do)
## Set up
First, you use following command,
```bash
$ bundle install
$ bundle exec rspec --init
```
Next, you remove comment out in `spec/spec_helper.rb`.
```ruby
.....
=begin (remove this)
....
=end (remove this)
```
